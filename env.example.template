# =============================================================================
# RAI SENTINEL - Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# cp env.example.template .env
# nano .env
# =============================================================================

# -----------------------------------------------------------------------------
# TELEGRAM CONFIGURATION
# -----------------------------------------------------------------------------

# Telegram Bot Token from @BotFather
# Format: 123456789:ABCdefGHIjklMNOpqrsTUVwxyz
# Validation: Must start with numbers, contain colon, alphanumeric after colon
# Example: 1234567890:ABCDEFghijklmnopQRSTUVwxyz1234567890
TG_TOKEN=your_telegram_bot_token_here

# Telegram Chat ID where alerts will be sent
# Format: Numeric string (can be negative for groups)
# Validation: Must be numeric (can start with minus for groups)
# Get it from: Send message to bot, then visit https://api.telegram.org/bot<TOKEN>/getUpdates
# Example: 123456789 or -1001234567890
TG_CHAT_ID=your_telegram_chat_id_here

# -----------------------------------------------------------------------------
# VALIDATOR ADDRESSES
# -----------------------------------------------------------------------------

# Wallet address (your validator's wallet)
# Format: bech32 address starting with 'republic1'
# Validation: Must start with 'republic1', length 42-45 characters, bech32 valid
# Example: republic1abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
WALLET_ADDRESS=republic1xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Validator operator address (valoper)
# Format: bech32 address starting with 'republicvaloper1'
# Validation: Must start with 'republicvaloper1', length 50-53 characters, bech32 valid
# Example: republicvaloper1abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
VALOPER_ADDRESS=republicvaloper1xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Consensus address (consaddr) - used for slashing info
# Format: bech32 address starting with 'republicvalcons1'
# Validation: Must start with 'republicvalcons1', length 50-53 characters, bech32 valid
# Example: republicvalcons1abc123def456ghi789jkl012mno345pqr678stu901vwx234yz
CONSADDR_ADDRESS=republicvalcons1xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# -----------------------------------------------------------------------------
# NETWORK CONFIGURATION
# -----------------------------------------------------------------------------

# RPC endpoint URL
# Format: HTTP/HTTPS URL with port
# Validation: Must be valid URL, start with http:// or https://, include port
# Default: http://localhost:26657
# Example: http://localhost:26657 or https://rpc.republicchain.io:443
RPC_URL=http://localhost:26657

# LCD (REST API) endpoint URL
# Format: HTTP/HTTPS URL with port
# Validation: Must be valid URL, start with http:// or https://, include port
# Default: http://localhost:1317
# Example: http://localhost:1317 or https://lcd.republicchain.io:443
LCD_URL=http://localhost:1317

# Chain ID for the network
# Format: String identifier
# Validation: Must be non-empty string, typically lowercase with hyphens
# Example: republic_1-1 or republic-testnet-1
CHAIN_ID=republic_1-1

# -----------------------------------------------------------------------------
# TOKEN CONFIGURATION
# -----------------------------------------------------------------------------

# Denomination of the native token
# Format: String (usually lowercase)
# Validation: Must be non-empty string, typically 3-10 characters
# Example: arai or urai
DENOM=arai

# Decimal places for token (for formatting)
# Format: Integer
# Validation: Must be positive integer, typically 6, 9, or 18
# Example: 18 (for arai with 18 decimals)
DECIMALS=18

# -----------------------------------------------------------------------------
# MONITORING CONFIGURATION
# -----------------------------------------------------------------------------

# Hours between heartbeat messages (normal status updates)
# Format: Integer or float
# Validation: Must be positive number > 0, recommended 1-24
# Example: 3 (send heartbeat every 3 hours)
HEARTBEAT_HOURS=3

# Percentage drop in rewards to trigger alert
# Format: Float
# Validation: Must be positive number, typically 1-50
# Example: 5.0 (alert if rewards drop by 5% or more)
REWARD_DROP_PCT=5.0

# Minutes before considering node height as stuck
# Format: Integer
# Validation: Must be positive integer, recommended 5-30
# Example: 10 (alert if height doesn't change for 10 minutes)
STUCK_MINUTES=10

# -----------------------------------------------------------------------------
# DIRECTORY CONFIGURATION
# -----------------------------------------------------------------------------

# Data directory for storing history and state files
# Format: Absolute path
# Validation: Must be valid absolute path, directory must exist or be creatable
# Default: ./history (relative to script location)
# Example: /root/rai-sentinel/history or /var/lib/rai-sentinel
DATA_DIR=./history

# Republic daemon home directory (for reference, not always used)
# Format: Absolute path
# Validation: Must be valid absolute path
# Default: /root/.republicd
# Example: /root/.republicd or /home/user/.republicd
REPUBLIC_HOME=/root/.republicd

# =============================================================================
# VALIDATION RULES SUMMARY
# =============================================================================
# 
# REQUIRED (must be set, no defaults):
#   - TG_TOKEN: Non-empty, contains colon
#   - TG_CHAT_ID: Numeric string
#   - WALLET_ADDRESS: Starts with 'republic1', bech32 valid
#   - VALOPER_ADDRESS: Starts with 'republicvaloper1', bech32 valid
#   - CONSADDR_ADDRESS: Starts with 'republicvalcons1', bech32 valid
#
# OPTIONAL (have defaults):
#   - RPC_URL: Defaults to http://localhost:26657
#   - LCD_URL: Defaults to http://localhost:1317
#   - CHAIN_ID: No default, but optional for monitoring
#   - DENOM: Defaults to 'arai'
#   - DECIMALS: Defaults to 18
#   - HEARTBEAT_HOURS: Defaults to 3
#   - REWARD_DROP_PCT: Defaults to 5.0
#   - STUCK_MINUTES: Defaults to 10
#   - DATA_DIR: Defaults to ./history
#   - REPUBLIC_HOME: Defaults to /root/.republicd
#
# =============================================================================


